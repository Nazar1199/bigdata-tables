{
  "name": "bigdata-tables",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "docker": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview"
  }
}
