# BigData Tables - Управление 1 млн элементов

## 🚀 Быстрый старт

### Запуск:
```bash
git clone https://github.com/Nazar1199/bigdata-tables
cd bigdata-tables
docker-compose up -d
```

**Откройте в браузере:**
- Frontend: http://localhost:5173
- Backend API: http://localhost:3000
- Документация: http://localhost:3000/api-docs

## 📋 Что делает проект?

Управление списком из **1 миллиона элементов** с двумя панелями:

**Левая панель** → Доступные элементы:
- Поиск по ID
- Добавление новых элементов
- Infinite Scroll (по 20 элементов)

**Правая панель** → Выбранные элементы:
- Drag & Drop сортировка
- Поиск по ID
- Infinite Scroll
- Сохранение состояния

## 🏗️ Ключевые подходы 

### 1. **Оптимизация данных**
- Пагинация
- Infinite Scroll с Intersection Observer
- В памяти на сервере 1M записей

### 2. **Умная синхронизация**
- Очереди с дедупликацией
- Батчинг: изменения - 1 сек, добавления - 10 сек
- Автосинхронизация каждые 10 сек

### 3. **Архитектура**
```
Frontend (Vue 3) → API → Очередь → Батчинг → Память
         ↑                                      ↓
         └────── Автосинх (10 сек) ────────────┘
```

### 4. **Особенности**
- **Без БД** - данные в памяти процесса
- **Общие для всех** - один экземпляр на всех пользователей

## 📁 Структура проекта
```
bigdata-tables/
├── frontend/     # Vue 3 + TypeScript + Pinia
├── backend/      # Express + Очереди + Батчинг
└── docker-compose.yml
```

## 🔧 API ключевые эндпоинты

```bash
# Получить элементы
GET /items?side=left&limit=20

# Выбрать элемент (батч 1 сек)
POST /items/select {id: 42}

# Добавить элемент (батч 10 сек)
POST /items/add {id: 1000001}

# Синхронизировать состояние
GET /sync
```

## 🎯 Технологии

**Frontend:**
- Vue 3 + TypeScript
- Pinia (состояние)
- Vue DnD Kit
- Axios

**Backend:**
- Express + TypeScript
- Очереди с Set/Map
- In-memory хранилище
- Swagger документация

## 💡 Пример использования

1. **Выбор элемента** - клик в левой панели
2. **Перетащите для сортировки** - в правой панели
3. **Поиск по ID** - введите число в поиск
4. **Добавление элементов** - введите ID + Enter слева